<!DOCTYPE html>
<html>
  <head>
    <title>SiteVagas</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <br><br>
    <% if user_signed_in? %>
      <%= current_user.email %>
      <% if current_user.email.include?('gmail') == false && current_user.email.include?('hotmail') == false %>
        <%= link_to 'Minha Empresa', company_path(current_user.company.id) %>
      <% else %>
        <%= link_to 'Meu perfil', account_path(current_user) %>
      <% end %>
      <%= link_to 'Sair', destroy_user_session_path, method: :delete %>
    <% else %>
      <%= link_to 'Registrar-se', new_user_registration_path %>
      <%= link_to 'Entrar', new_user_session_path %>
    <% end %>


    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

    <%= yield %>
  </body>
</html>